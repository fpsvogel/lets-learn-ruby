---
layout: default
title: Let's Learn Ruby
---

<%= render Curriculum.new(
  curriculum: site.data.curriculum,
  title: site.config.title,
  subtitle: site.config.subtitle,
  callout: site.config.callout
) %>
